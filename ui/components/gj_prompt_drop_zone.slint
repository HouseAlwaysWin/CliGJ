import { TextEdit } from "std-widgets.slint";
import { AppTheme } from "../theme.slint";

/// Bottom prompt bar: input + function bar (Cursor/VSCode style).
export component GjPromptDropZone inherits Rectangle {
    in-out property <string> prompt;
    in property <bool> raw-input-enabled: false;
    in property <bool> has-image;
    in property <image> preview-image;
    in property <string> selected-context;
    callback submit-requested;
    callback history-prev-requested;
    callback history-next-requested;
    callback control-sequence-requested(string);
    callback raw-input-toggle-requested;
    callback inject-file-requested;

    horizontal-stretch: 1;
    min-height: 116px;
    max-height: 116px;
    background: AppTheme.bg;
    border-width: 0px;

    Rectangle {
        x: 0px;
        y: 0px;
        width: parent.width;
        height: parent.height;
        background: AppTheme.bg;

        Rectangle {
            x: 0px;
            y: 0px;
            width: parent.width;
            height: parent.height;
            background: AppTheme.prompt_chrome_bg;
            border-width: 1px;
            border-color: AppTheme.prompt_chrome_border;
            border-radius: AppTheme.prompt_chrome_radius;

            VerticalLayout {
                padding: 8px;
                spacing: 4px;

                TextEdit {
                    horizontal-stretch: 1;
                    min-height: 68px;
                    text <=> root.prompt;
                    placeholder-text: "Plan, Build, / for commands, @ for context";
                    font-size: 13px;
                    wrap: word-wrap;
                    key-pressed(event) => {
                        if (event.modifiers.control && (event.text == "r" || event.text == "R")) {
                            root.raw-input-toggle-requested();
                            accept
                        } else if (event.modifiers.alt && event.text == Key.UpArrow) {
                            root.control-sequence-requested("\u{001b}[A");
                            accept
                        } else if (event.modifiers.alt && event.text == Key.DownArrow) {
                            root.control-sequence-requested("\u{001b}[B");
                            accept
                        } else if (event.modifiers.alt && event.text == Key.RightArrow) {
                            root.control-sequence-requested("\u{001b}[C");
                            accept
                        } else if (event.modifiers.alt && event.text == Key.LeftArrow) {
                            root.control-sequence-requested("\u{001b}[D");
                            accept
                        } else if (root.raw-input-enabled && (event.text == Key.Return || event.text == "\n" || event.text == "\u{000d}") && !event.modifiers.shift) {
                            root.control-sequence-requested("\u{000d}");
                            accept
                        } else if (root.raw-input-enabled && event.text == Key.UpArrow) {
                            root.control-sequence-requested("\u{001b}[A");
                            accept
                        } else if (root.raw-input-enabled && event.text == Key.DownArrow) {
                            root.control-sequence-requested("\u{001b}[B");
                            accept
                        } else if (root.raw-input-enabled && event.text == Key.RightArrow) {
                            root.control-sequence-requested("\u{001b}[C");
                            accept
                        } else if (root.raw-input-enabled && event.text == Key.LeftArrow) {
                            root.control-sequence-requested("\u{001b}[D");
                            accept
                        } else if (root.raw-input-enabled && event.text == Key.Home) {
                            root.control-sequence-requested("\u{001b}[H");
                            accept
                        } else if (root.raw-input-enabled && event.text == Key.End) {
                            root.control-sequence-requested("\u{001b}[F");
                            accept
                        } else if (root.raw-input-enabled && event.text == Key.PageUp) {
                            root.control-sequence-requested("\u{001b}[5~");
                            accept
                        } else if (root.raw-input-enabled && event.text == Key.PageDown) {
                            root.control-sequence-requested("\u{001b}[6~");
                            accept
                        } else if (root.raw-input-enabled && event.text == Key.Delete) {
                            root.control-sequence-requested("\u{001b}[3~");
                            accept
                        } else if (root.raw-input-enabled && event.text == Key.Escape) {
                            root.control-sequence-requested("\u{001b}");
                            accept
                        } else if ((event.text == Key.Return || event.text == "\n" || event.text == "\u{000d}") && !event.modifiers.shift) {
                            root.submit-requested();
                            accept
                        } else if (!root.raw-input-enabled && event.text == Key.UpArrow) {
                            root.history-prev-requested();
                            accept
                        } else if (!root.raw-input-enabled && event.text == Key.DownArrow) {
                            root.history-next-requested();
                            accept
                        } else if (event.modifiers.control && (event.text == "c" || event.text == "C")) {
                            root.control-sequence-requested("\u{0003}");
                            accept
                        } else if (event.text == Key.Tab) {
                            root.control-sequence-requested("\u{0009}");
                            accept
                        } else {
                            reject
                        }
                    }
                }

                HorizontalLayout {
                    spacing: 8px;

                    Rectangle {
                        min-width: 78px;
                        height: 22px;
                        border-width: 1px;
                        border-color: AppTheme.prompt_chrome_border;
                        border-radius: 11px;
                        background: #00000000;

                        Text {
                            text: "Agent";
                            color: AppTheme.text_muted;
                            horizontal-alignment: center;
                            vertical-alignment: center;
                            font-size: 11px;
                        }
                    }

                    Rectangle {
                        min-width: 54px;
                        height: 22px;
                        border-width: 1px;
                        border-color: AppTheme.prompt_chrome_border;
                        border-radius: 11px;
                        background: #00000000;

                        Text {
                            text: "Auto";
                            color: AppTheme.text_muted;
                            horizontal-alignment: center;
                            vertical-alignment: center;
                            font-size: 11px;
                        }
                    }

                    Rectangle {
                        horizontal-stretch: 1;
                        background: #00000000;
                    }

                    Rectangle {
                        min-width: 56px;
                        height: 22px;
                        border-width: 1px;
                        border-color: root.raw-input-enabled ? AppTheme.accent : AppTheme.prompt_chrome_border;
                        border-radius: 11px;
                        background: root.raw-input-enabled ? #ff4f6f : #00000000;

                        Text {
                            text: root.raw-input-enabled ? "R ON" : "R OFF";
                            color: root.raw-input-enabled ? AppTheme.text : AppTheme.text_muted;
                            horizontal-alignment: center;
                            vertical-alignment: center;
                            font-size: 10px;
                        }

                        TouchArea {
                            clicked => {
                                root.raw-input-toggle-requested();
                            }
                        }
                    }

                    Rectangle {
                        min-width: 40px;
                        height: 22px;
                        border-width: 1px;
                        border-color: AppTheme.prompt_chrome_border;
                        border-radius: 11px;
                        background: #00000000;

                        Text {
                            text: "File";
                            color: AppTheme.text_muted;
                            horizontal-alignment: center;
                            vertical-alignment: center;
                            font-size: 11px;
                        }

                        inject_file_ta := TouchArea {
                            width: parent.width;
                            height: parent.height;
                            clicked => {
                                root.inject-file-requested();
                            }
                        }
                    }

                    Rectangle {
                        min-width: 36px;
                        height: 22px;
                        border-width: 1px;
                        border-color: AppTheme.prompt_chrome_border;
                        border-radius: 11px;
                        background: #00000000;

                        Text {
                            text: "Img";
                            color: AppTheme.text_muted;
                            horizontal-alignment: center;
                            vertical-alignment: center;
                            font-size: 11px;
                        }
                    }

                    Rectangle {
                        min-width: 34px;
                        height: 22px;
                        border-width: 1px;
                        border-color: AppTheme.prompt_chrome_border;
                        border-radius: 11px;
                        background: #00000000;

                        Text {
                            text: "Mic";
                            color: AppTheme.text_muted;
                            horizontal-alignment: center;
                            vertical-alignment: center;
                            font-size: 11px;
                        }
                    }

                    Rectangle {
                        width: 34px;
                        height: 22px;
                        border-width: 1px;
                        border-color: AppTheme.accent;
                        border-radius: 11px;
                        background: submit_ta.has-hover ? #ff4f6f : AppTheme.accent;

                        Text {
                            text: "↑";
                            color: AppTheme.text;
                            horizontal-alignment: center;
                            vertical-alignment: center;
                            font-size: 12px;
                            font-weight: 700;
                        }

                        submit_ta := TouchArea {
                            width: parent.width;
                            height: parent.height;
                            clicked => {
                                root.submit-requested();
                            }
                        }
                    }
                }
            }
        }
    }
}
